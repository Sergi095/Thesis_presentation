services:
  app:
    build: .
    container_name: thesis_app
    restart: unless-stopped
    ports:
      - "8000:8000"
    volumes:
      - ./assets:/app/assets:ro
    environment:
      - PYTHONUNBUFFERED=1
